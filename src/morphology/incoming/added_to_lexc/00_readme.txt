This directory contains the lists of former incoming data
that have been added to the lexc files.

These are categorized by date added to lexc, i.e., in separate directory
with the addition date in the directory name.

